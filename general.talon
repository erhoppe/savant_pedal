os: mac
-
key(cmd-ctrl-shift-l): user.smooth_wheel_scroll("down")

key(cmd-ctrl-shift-r): user.smooth_wheel_scroll("up")

key(cmd-ctrl-shift-c): key(cmd-space)

##adding alt is the release signal for the pedals
